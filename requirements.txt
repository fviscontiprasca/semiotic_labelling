## PIP-ONLY INSTALL (Python 3.13)
## Follow PyTorch "Start Locally" guidance: use --index-url for the selected CUDA wheels
--index-url https://download.pytorch.org/whl/cu129

# Core ML Framework (pip wheels)
torch==2.8.0
torchvision==0.23.0
torchaudio==2.8.0

# Hugging Face Ecosystem - Version aligned
transformers==4.44.1  # Stable on Python 3.13
diffusers==0.30.3  # Flux.1 support
datasets==3.3.2  # Latest stable
accelerate==1.10.1  # PyTorch 2.8 + Python 3.13 support
huggingface_hub>=0.34.0,<0.35.0  # API stability
safetensors==0.5.3  # Model compatibility
peft==0.17.1  # LoRA fine-tuning

# Computer Vision & Image Processing
ultralytics==8.3.199  # YOLO11 with segmentation
opencv-python==4.10.0  # CV operations (pip)
Pillow==11.3.0  # Latest PIL

# Text-Image Alignment & Embeddings
# clip-by-openai>=1.0  # REMOVED: conflicts with transformers
sentence-transformers==5.1.0  # Latest with improved embeddings

# Dataset Management
# FiftyOne is heavy; prefer pip and allow minor drift across Python 3.13 builds
fiftyone>=1.4,<1.9  # MongoDB-backed dataset management (pip)
pymongo>=4.9.0,<5.0.0  # Required by FiftyOne

# Utilities & Performance
jsonlines==4.0.0  # Data format support
# Optional/Platform-specific extras (commented by default)
# xformers==0.0.32.post2
# bitsandbytes==0.47.0
wandb==0.21.4  # Experiment tracking

# Data Processing & Analysis - Compatible versions
numpy>=2.1.3,<3  # Python 3.13 requires NumPy 2.x
pandas==2.3.2  # Data manipulation
scikit-learn==1.7.2  # ML utilities
matplotlib==3.10.6  # Plotting
seaborn>=0.12.0,<0.14.0  # Statistical plots

# Development & Logging
tqdm==4.67.1  # Progress bars
typing_extensions>=4.15.0  # Type hints support
